# FBP 엔진 아키텍처

본 문서는 FBP(Flow-Based Programming) 엔진의 아키텍처를 설명합니다.

## 1. 핵심 구성 요소

엔진은 `노드(Node)`, `포트(Port)`, `연결(Connection)`, `플로우(Flow)`라는 네 가지 핵심 개념으로 구성됩니다.

### 1.1. `메시지 (Message)`
- 시스템을 흐르는 데이터의 기본 단위입니다.
- 고유 ID(UUID), 타임스탬프, 그리고 `Map<String, Object>` 형태의 페이로드(payload)를 포함합니다.
- 불변(Immutable) 객체로 설계되어, 수정 시 새로운 `Message` 인스턴스가 생성됩니다.

### 1.2. `노드 (Node)`
- 기본 처리 단위이며, 모든 노드는 `AbstractNode`를 상속받습니다.
- 이름이 지정된 `입력 포트(InputPort)`와 `출력 포트(OutputPort)`를 가집니다.
- 실제 처리 로직은 `onProcess(portName, message)` 메서드에 구현합니다.
- 모든 노드는 예외 처리를 위해 `_error`라는 이름의 출력 포트를 기본적으로 가집니다.

### 1.3. `포트 (Port)`
- 노드에서 메시지가 들어오고 나가는 지점입니다.
- `출력 포트`는 하나 이상의 `연결`로 이어질 수 있습니다.
- `입력 포트`는 `연결`로부터 메시지를 수신합니다.

### 1.4. `연결 (Connection)`
- `출력 포트`와 `입력 포트`를 잇는 버퍼가 있는 단방향 채널입니다.
- `java.util.concurrent.BlockingQueue`로 구현됩니다.
- 큐 오버플로우 상황을 제어하기 위해 `Block`, `DropOldest`, `DropNewest` 같은 **백프레셔(Backpressure)** 전략을 지원합니다.

### 1.5. `플로우 (Flow)`
- `노드` 인스턴스와 이를 잇는 `연결`의 집합입니다.
- 하나의 완전하고 실행 가능한 처리 단계의 그래프를 나타냅니다.
- 순환 참조나 고아 연결(orphaned connection) 같은 구조적 오류를 검증하는 기능을 제공합니다.

## 2. 엔진 및 실행

### 2.1. `FlowRunner`
- 단일 `플로우`의 실행을 책임집니다.
- 시작 시, 플로우 내의 각 `연결`마다 스레드를 하나씩 할당합니다. 각 스레드는 자신의 `연결`에서 새 메시지를 기다리다가, 메시지가 도착하면 대상 `입력 포트`로 전달하는 루프를 실행합니다.
- 자신의 플로우에 속한 모든 `노드`의 생명주기(`initialize`, `shutdown`)를 관리합니다.

### 2.2. `FlowEngine`
- 하나 이상의 `FlowRunner`를 관리하는 상위 컨테이너입니다.
- 자신에게 등록된 플로우들을 시작하고 중지시키는 역할을 담당합니다.

### 2.3. `FlowManager`
- 애플리케이션 내의 모든 `FlowEngine`을 관리하는 싱글턴(Singleton) 객체입니다.
- 플로우의 배포, 시작, 중지, 제거를 위한 중앙 집중화된 접점을 제공합니다.
- 사용 가능한 노드 타입을 등록하는 `NodeRegistry`를 노출합니다.

### 2.4. `NodeRegistry`
- `노드 타입 이름(typeName)`과 `노드 생성기(NodeFactory)`를 매핑하여 관리합니다.
- `FlowManager`가 플로우 정의(`FlowDefinition`)를 기반으로 `노드` 인스턴스를 생성할 때 사용됩니다.

## 3. 데이터 흐름 및 스레딩 모델

- **연결당-스레드(Thread-per-Connection)** 모델을 사용합니다. `FlowRunner`는 각 `연결`에 대한 처리 루프를 `ThreadPoolExecutor`에 제출합니다.
- 이 루프는 `connection.poll()` (내부적으로 `queue.take()`)을 호출하며 메시지가 도착할 때까지 대기(Blocking)합니다.
- 상위 노드가 `send()`를 호출하면 메시지는 `연결`의 큐에 추가됩니다.
- 대기 중이던 `연결` 스레드는 깨어나 메시지를 가져온 후, 타겟 `노드`의 `process()` 메서드를 호출합니다.
- 이 모델은 각 노드의 처리를 격리하여, 특정 노드의 지연이 다른 노드에 영향을 주지 않도록 합니다. 스레드 풀은 `ThreadPoolConfig`를 통해 세부 설정이 가능합니다.

## 4. 고급 패턴

### 4.1. 에러 핸들링
- 노드의 `onProcess()`에서 발생하는 모든 예외는 `AbstractNode`가 잡아냅니다.
- 예외와 원본 메시지는 `ErrorMessage` 객체로 포장됩니다.
- 이 `ErrorMessage`는 해당 노드의 `_error` 출력 포트로 보내집니다.
- 만약 `_error` 포트가 다른 노드와 연결되어 있다면, 에러는 별도의 처리 플로우로 흐르게 됩니다. 연결되어 있지 않으면, 에러는 로그로만 기록됩니다.

### 4.2. 백프레셔 (Backpressure)
- 각 `연결`은 `용량(capacity)`과 `백프레셔 전략(backpressure strategy)`을 가질 수 있습니다.
- 큐가 가득 찼을 때, 설정된 전략에 따라 동작이 결정됩니다.
    - `Block`: 생산자 스레드가 큐에 여유 공간이 생길 때까지 대기합니다.
    - `DropOldest`: 큐에서 가장 오래된 메시지를 버리고 새 메시지를 추가합니다.
    - `DropNewest`: 새로 들어온 메시지를 버립니다.

### 4.3. 동적 라우팅
- `DynamicRouterNode`는 메시지의 내용에 따라 출력을 보낼 포트를 동적으로 결정합니다.
- 라우팅 규칙(`RoutingRule`) 목록을 설정으로 받으며, 첫 번째로 일치하는 규칙의 포트로 메시지를 보냅니다. 일치하는 규칙이 없으면 `default` 포트를 사용합니다.

### 4.4. 서브플로우 (SubFlow)
- `SubFlowNode`는 단일 노드처럼 보이지만, 내부에 완전한 `플로우`를 캡슐화합니다.
- 플로우 정의의 `publicPorts` 속성을 통해 `SubFlowNode`의 포트와 내부 플로우의 노드 포트를 매핑할 수 있습니다.
- 내부 플로우에서 발생한 에러는 `SubFlowNode`의 `_error` 포트로 자동 전파됩니다.

## 5. 관리 및 모니터링

- JDK에 내장된 `HttpServer`를 사용하여 REST API를 제공합니다.
- **주요 엔드포인트**:
    - `GET /health`: 엔진 상태, 가동 시간, 플로우 개수를 제공합니다.
    - `POST, GET, DELETE /flows`: 플로우를 배포, 조회, 삭제합니다.
    - `GET /flows/{id}/metrics`: 특정 플로우의 노드별 처리량, 에러 수, 평균 처리 시간 등 메트릭을 제공합니다.
- **메트릭 수집**:
    - `MetricsCollector` 싱글턴 객체가 모든 노드의 메트릭을 수집합니다.
    - `AbstractNode`는 모든 `process()` 호출의 실행 시간과 성공/실패 여부를 자동으로 기록합니다.
    - 수집된 데이터는 스레드에 안전한 인메모리 맵에 저장됩니다.

### 5.1. 동적 로깅 제어 (Global Debug Mode)
- 초당 수만 건 이상의 메시지를 처리하는 고성능 환경에서는 로깅 자체가 심각한 I/O 병목을 유발할 수 있습니다.
- 이를 방지하기 위해 엔진은 '전역 디버그 모드(Global Debug Mode)'를 지원합니다.
- 평상시에는 메시지 흐름에 대한 상세 로그를 남기지 않아 최고의 성능을 유지합니다 (`AbstractNode.process()` 레벨).
- 시스템 운영자는 보안이 확보된 서버 내부 관리 채널을 통해 `AbstractNode.setGlobalDebugMode(true)`를 호출하여, 엔진을 중단하지 않고 실시간으로 상세 로깅을 활성화할 수 있습니다.
- 이는 악의적인 클라이언트의 API 호출로 인한 시스템 부하(DoS) 위험을 차단하고, 운영상의 안전성을 확보하기 위한 설계입니다.
